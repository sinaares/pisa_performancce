version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - backend/.env
    environment:
      - PORT=8000
      - WORKERS=1
    volumes:
      - ./data:/app/data:ro
    restart: unless-stopped
